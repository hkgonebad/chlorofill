const CONFIG = {
	FLIPKART: {
		// These will be handled by the proxy server
		AFFILIATE_ID: window.env?.FLIPKART_AFFILIATE_ID || "",
		AFFILIATE_TOKEN: window.env?.FLIPKART_AFFILIATE_TOKEN || "",
	},
};
